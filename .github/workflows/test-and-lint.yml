name: Test & Lint

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'templates/project-templates/nextjs-starter/package-lock.json'

      - name: Install dependencies
        working-directory: templates/project-templates/nextjs-starter
        run: npm ci

      - name: Run ESLint
        working-directory: templates/project-templates/nextjs-starter
        run: npm run lint

      - name: Check TypeScript
        working-directory: templates/project-templates/nextjs-starter
        run: npm run type-check

  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'templates/project-templates/nextjs-starter/package-lock.json'

      - name: Install dependencies
        working-directory: templates/project-templates/nextjs-starter
        run: npm ci

      - name: Run tests
        working-directory: templates/project-templates/nextjs-starter
        run: npm run test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: templates/project-templates/nextjs-starter/coverage/coverage-final.json
          fail_ci_if_error: false

  build:
    name: Build Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'templates/project-templates/nextjs-starter/package-lock.json'

      - name: Install dependencies
        working-directory: templates/project-templates/nextjs-starter
        run: npm ci

      - name: Build project
        working-directory: templates/project-templates/nextjs-starter
        run: npm run build
        env:
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: templates/project-templates/nextjs-starter/.next
